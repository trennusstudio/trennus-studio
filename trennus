<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Trennus Studio — Renderizações Cinematográficas com IA</title>
  <meta name="description" content="Crie renders arquitetônicos ultra realistas com Inteligência Artificial. Curso premium + 100 Prompts prontos." />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Barlow:wght@200;300;400;500;600&family=Barlow+Condensed:wght@200;300;400;500;600;700&display=swap" rel="stylesheet" />
  <style>
    /* =============================================
       CSS VARIABLES & RESET
    ============================================= */
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg: #080808;
      --bg2: #0d0d0d;
      --bg3: #111111;
      --surface: #161616;
      --border: rgba(255,255,255,0.06);
      --border-hover: rgba(255,255,255,0.15);
      --gold: #C49A5A;
      --gold2: #E8C97A;
      --gold-dim: rgba(196,154,90,0.15);
      --white: #FAFAF8;
      --white-dim: rgba(250,250,248,0.55);
      --white-faint: rgba(250,250,248,0.12);
      --font-display: 'Cormorant Garamond', serif;
      --font-body: 'Barlow', sans-serif;
      --font-condensed: 'Barlow Condensed', sans-serif;
      --transition: cubic-bezier(0.25, 0.46, 0.45, 0.94);
      --ease-out: cubic-bezier(0.0, 0.0, 0.2, 1);
    }

    html { scroll-behavior: smooth; }

    body {
      background: var(--bg);
      color: var(--white);
      font-family: var(--font-body);
      font-weight: 300;
      overflow-x: hidden;
      cursor: default;
    }

    img { display: block; width: 100%; height: 100%; object-fit: cover; }
    a { color: inherit; text-decoration: none; }

    /* =============================================
       CUSTOM CURSOR
    ============================================= */
    .cursor {
      width: 8px; height: 8px;
      background: var(--gold);
      border-radius: 50%;
      position: fixed;
      top: 0; left: 0;
      pointer-events: none;
      z-index: 9999;
      transform: translate(-50%, -50%);
      transition: transform 0.1s, width 0.3s, height 0.3s, background 0.3s;
    }
    .cursor-ring {
      width: 36px; height: 36px;
      border: 1px solid rgba(196,154,90,0.5);
      border-radius: 50%;
      position: fixed;
      top: 0; left: 0;
      pointer-events: none;
      z-index: 9998;
      transform: translate(-50%, -50%);
      transition: all 0.15s var(--transition);
    }
    body:hover .cursor { opacity: 1; }

    /* =============================================
       GRAIN OVERLAY
    ============================================= */
    .grain {
      position: fixed;
      inset: -200%;
      width: 400%; height: 400%;
      pointer-events: none;
      z-index: 1000;
      opacity: 0.025;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
      animation: grain 0.5s steps(1) infinite;
    }
    @keyframes grain {
      0%,100%{transform:translate(0,0)} 10%{transform:translate(-2%,-3%)} 20%{transform:translate(3%,2%)}
      30%{transform:translate(-4%,1%)} 40%{transform:translate(2%,-2%)} 50%{transform:translate(-1%,4%)}
      60%{transform:translate(3%,-1%)} 70%{transform:translate(-3%,3%)} 80%{transform:translate(2%,-4%)}
      90%{transform:translate(-2%,2%)}
    }

    /* =============================================
       NAVBAR
    ============================================= */
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 500;
      padding: 28px 60px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      transition: background 0.6s, padding 0.4s;
    }
    nav.scrolled {
      background: rgba(8,8,8,0.92);
      backdrop-filter: blur(20px);
      padding: 18px 60px;
      border-bottom: 1px solid var(--border);
    }

    .nav-logo {
      display: flex;
      align-items: center;
      gap: 14px;
    }
    .nav-logo img {
      /* Logo image */
      width: 38px; height: 38px;
      object-fit: contain;
      filter: brightness(1.1);
    }
    .nav-logo-text {
      font-family: var(--font-condensed);
      font-size: 18px;
      font-weight: 500;
      letter-spacing: 0.2em;
      color: var(--white);
    }
    .nav-logo-text span {
      color: var(--gold);
    }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 48px;
      list-style: none;
    }
    .nav-links a {
      font-family: var(--font-condensed);
      font-size: 11px;
      font-weight: 400;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--white-dim);
      transition: color 0.3s;
      position: relative;
    }
    .nav-links a::after {
      content: '';
      position: absolute;
      bottom: -4px; left: 0;
      width: 0; height: 1px;
      background: var(--gold);
      transition: width 0.3s var(--transition);
    }
    .nav-links a:hover { color: var(--white); }
    .nav-links a:hover::after { width: 100%; }

    .nav-cta {
      font-family: var(--font-condensed);
      font-size: 11px;
      font-weight: 500;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--gold);
      border: 1px solid var(--gold);
      padding: 10px 24px;
      transition: background 0.3s, color 0.3s;
    }
    .nav-cta:hover {
      background: var(--gold);
      color: var(--bg);
    }

    /* =============================================
       HERO SECTION
    ============================================= */
    #hero {
      position: relative;
      height: 100vh;
      min-height: 700px;
      display: flex;
      align-items: flex-end;
      overflow: hidden;
    }

    .hero-bg {
      position: absolute;
      inset: 0;
      z-index: 0;
    }
    /* ↓ TROCAR IMAGEM HERO AQUI ↓ */
    .hero-bg img {
      width: 100%; height: 100%;
      object-fit: cover;
      object-position: center;
      transform: scale(1.05);
      animation: heroScale 8s var(--ease-out) forwards;
    }
    @keyframes heroScale {
      from { transform: scale(1.05); }
      to { transform: scale(1.0); }
    }

    .hero-overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(
        to top,
        rgba(8,8,8,0.98) 0%,
        rgba(8,8,8,0.65) 40%,
        rgba(8,8,8,0.2) 75%,
        rgba(8,8,8,0.05) 100%
      );
      z-index: 1;
    }
    .hero-overlay-left {
      position: absolute;
      inset: 0;
      background: linear-gradient(
        to right,
        rgba(8,8,8,0.7) 0%,
        transparent 60%
      );
      z-index: 1;
    }

    .hero-content {
      position: relative;
      z-index: 2;
      padding: 0 60px 90px;
      max-width: 860px;
    }

    .hero-eyebrow {
      font-family: var(--font-condensed);
      font-size: 11px;
      font-weight: 400;
      letter-spacing: 0.35em;
      text-transform: uppercase;
      color: var(--gold);
      margin-bottom: 28px;
      opacity: 0;
      animation: fadeUp 0.8s 0.3s var(--transition) forwards;
      display: flex;
      align-items: center;
      gap: 16px;
    }
    .hero-eyebrow::before {
      content: '';
      display: block;
      width: 40px; height: 1px;
      background: var(--gold);
    }

    .hero-h1 {
      font-family: var(--font-display);
      font-size: clamp(44px, 6vw, 88px);
      font-weight: 300;
      line-height: 1.0;
      letter-spacing: -0.01em;
      color: var(--white);
      margin-bottom: 28px;
      opacity: 0;
      animation: fadeUp 0.9s 0.5s var(--transition) forwards;
    }
    .hero-h1 em {
      font-style: italic;
      color: var(--gold2);
    }

    .hero-sub {
      font-family: var(--font-body);
      font-size: clamp(14px, 1.5vw, 18px);
      font-weight: 300;
      line-height: 1.7;
      color: var(--white-dim);
      max-width: 520px;
      margin-bottom: 52px;
      opacity: 0;
      animation: fadeUp 0.9s 0.7s var(--transition) forwards;
    }

    .hero-btns {
      display: flex;
      gap: 20px;
      flex-wrap: wrap;
      opacity: 0;
      animation: fadeUp 0.9s 0.9s var(--transition) forwards;
    }

    .btn-primary {
      font-family: var(--font-condensed);
      font-size: 12px;
      font-weight: 500;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      background: var(--gold);
      color: var(--bg);
      padding: 16px 40px;
      border: 1px solid var(--gold);
      transition: all 0.35s var(--transition);
      display: inline-block;
      position: relative;
      overflow: hidden;
    }
    .btn-primary::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--white);
      transform: translateX(-101%);
      transition: transform 0.35s var(--transition);
    }
    .btn-primary:hover::before { transform: translateX(0); }
    .btn-primary span { position: relative; z-index: 1; }

    .btn-outline {
      font-family: var(--font-condensed);
      font-size: 12px;
      font-weight: 400;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--white);
      padding: 16px 40px;
      border: 1px solid var(--border-hover);
      transition: all 0.35s var(--transition);
      display: inline-block;
    }
    .btn-outline:hover {
      border-color: var(--gold);
      color: var(--gold);
    }

    .hero-scroll {
      position: absolute;
      bottom: 40px;
      right: 60px;
      z-index: 2;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 10px;
      opacity: 0;
      animation: fadeIn 1s 1.4s forwards;
    }
    .hero-scroll span {
      font-family: var(--font-condensed);
      font-size: 9px;
      letter-spacing: 0.3em;
      text-transform: uppercase;
      color: var(--white-dim);
      writing-mode: vertical-lr;
    }
    .scroll-line {
      width: 1px;
      height: 60px;
      background: linear-gradient(to bottom, var(--gold), transparent);
      animation: scrollLine 2s 1.8s infinite;
    }
    @keyframes scrollLine {
      0% { transform: scaleY(0); transform-origin: top; opacity: 1; }
      50% { transform: scaleY(1); transform-origin: top; opacity: 1; }
      51% { transform: scaleY(1); transform-origin: bottom; }
      100% { transform: scaleY(0); transform-origin: bottom; opacity: 0.3; }
    }

    /* =============================================
       MARQUEE
    ============================================= */
    .marquee-section {
      border-top: 1px solid var(--border);
      border-bottom: 1px solid var(--border);
      overflow: hidden;
      padding: 20px 0;
      background: var(--bg2);
    }
    .marquee-track {
      display: flex;
      gap: 0;
      animation: marquee 30s linear infinite;
      white-space: nowrap;
    }
    .marquee-item {
      font-family: var(--font-condensed);
      font-size: 11px;
      font-weight: 300;
      letter-spacing: 0.3em;
      text-transform: uppercase;
      color: var(--white-dim);
      padding: 0 48px;
      display: flex;
      align-items: center;
      gap: 48px;
    }
    .marquee-item::after {
      content: '·';
      color: var(--gold);
      font-size: 16px;
    }
    @keyframes marquee {
      from { transform: translateX(0); }
      to { transform: translateX(-50%); }
    }

    /* =============================================
       SECTION COMMONS
    ============================================= */
    section { position: relative; }

    .section-label {
      font-family: var(--font-condensed);
      font-size: 10px;
      font-weight: 400;
      letter-spacing: 0.4em;
      text-transform: uppercase;
      color: var(--gold);
      margin-bottom: 20px;
      display: flex;
      align-items: center;
      gap: 14px;
    }
    .section-label::before {
      content: '';
      display: block;
      width: 30px; height: 1px;
      background: var(--gold);
    }

    .section-h2 {
      font-family: var(--font-display);
      font-size: clamp(36px, 4vw, 64px);
      font-weight: 300;
      line-height: 1.1;
      letter-spacing: -0.01em;
      color: var(--white);
    }
    .section-h2 em { font-style: italic; color: var(--gold2); }

    /* Reveal animation */
    .reveal {
      opacity: 0;
      transform: translateY(40px);
      transition: opacity 0.9s var(--transition), transform 0.9s var(--transition);
    }
    .reveal.visible {
      opacity: 1;
      transform: translateY(0);
    }
    .reveal-delay-1 { transition-delay: 0.1s; }
    .reveal-delay-2 { transition-delay: 0.2s; }
    .reveal-delay-3 { transition-delay: 0.3s; }
    .reveal-delay-4 { transition-delay: 0.4s; }

    /* =============================================
       GALLERY SECTION
    ============================================= */
    #gallery {
      padding: 120px 60px;
      background: var(--bg);
    }

    .gallery-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      margin-bottom: 70px;
    }
    .gallery-header-text {}
    .gallery-count {
      font-family: var(--font-condensed);
      font-size: 80px;
      font-weight: 200;
      color: var(--border);
      line-height: 1;
      letter-spacing: -0.04em;
    }

    /* Masonry-style grid */
    .gallery-grid {
      display: grid;
      grid-template-columns: repeat(12, 1fr);
      grid-template-rows: auto;
      gap: 12px;
    }

    .gallery-item {
      position: relative;
      overflow: hidden;
      cursor: pointer;
      background: var(--surface);
    }

    /* Grid layout for items */
    .gallery-item:nth-child(1) { grid-column: 1/7; grid-row: 1/2; aspect-ratio: 16/9; }
    .gallery-item:nth-child(2) { grid-column: 7/13; grid-row: 1/2; aspect-ratio: 16/9; }
    .gallery-item:nth-child(3) { grid-column: 1/5; grid-row: 2/3; aspect-ratio: 4/3; }
    .gallery-item:nth-child(4) { grid-column: 5/9; grid-row: 2/3; aspect-ratio: 4/3; }
    .gallery-item:nth-child(5) { grid-column: 9/13; grid-row: 2/3; aspect-ratio: 4/3; }
    .gallery-item:nth-child(6) { grid-column: 1/8; grid-row: 3/4; aspect-ratio: 16/7; }
    .gallery-item:nth-child(7) { grid-column: 8/13; grid-row: 3/4; aspect-ratio: 16/10; }
    .gallery-item:nth-child(8) { grid-column: 1/5; grid-row: 4/5; aspect-ratio: 4/3; }
    .gallery-item:nth-child(9) { grid-column: 5/9; grid-row: 4/5; aspect-ratio: 4/3; }
    .gallery-item:nth-child(10) { grid-column: 9/13; grid-row: 4/5; aspect-ratio: 4/3; }

    .gallery-item img {
      width: 100%; height: 100%;
      object-fit: cover;
      transition: transform 0.9s var(--transition), filter 0.6s;
      filter: brightness(0.92) saturate(0.9);
    }
    .gallery-item:hover img {
      transform: scale(1.06);
      filter: brightness(1) saturate(1.1);
    }

    .gallery-item-overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(to top, rgba(8,8,8,0.7) 0%, transparent 50%);
      opacity: 0;
      transition: opacity 0.4s;
      display: flex;
      align-items: flex-end;
      padding: 24px;
    }
    .gallery-item:hover .gallery-item-overlay { opacity: 1; }

    .gallery-item-label {
      font-family: var(--font-condensed);
      font-size: 11px;
      font-weight: 300;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--white-dim);
    }

    /* Gold accent line on hover */
    .gallery-item::after {
      content: '';
      position: absolute;
      bottom: 0; left: 0;
      width: 0; height: 2px;
      background: var(--gold);
      transition: width 0.5s var(--transition);
      z-index: 3;
    }
    .gallery-item:hover::after { width: 100%; }

    /* =============================================
       ABOUT / STUDIO SECTION
    ============================================= */
    #about {
      padding: 140px 60px;
      background: var(--bg2);
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 100px;
      align-items: center;
    }

    .about-visual {
      position: relative;
      aspect-ratio: 4/5;
    }
    /* ↓ IMAGEM SOBRE AQUI ↓ */
    .about-visual-main {
      width: 100%; height: 100%;
      overflow: hidden;
    }
    .about-visual-main img {
      width: 100%; height: 100%;
      object-fit: cover;
      filter: brightness(0.85) saturate(0.8);
    }

    .about-visual-badge {
      position: absolute;
      bottom: -24px;
      right: -24px;
      width: 150px; height: 150px;
      background: var(--gold-dim);
      border: 1px solid var(--gold);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 4px;
      backdrop-filter: blur(10px);
    }
    .badge-number {
      font-family: var(--font-display);
      font-size: 52px;
      font-weight: 300;
      color: var(--gold2);
      line-height: 1;
    }
    .badge-text {
      font-family: var(--font-condensed);
      font-size: 10px;
      font-weight: 300;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--white-dim);
      text-align: center;
    }

    .about-content {}
    .about-text {
      margin-top: 32px;
      font-size: 16px;
      line-height: 1.9;
      color: var(--white-dim);
      font-weight: 300;
    }
    .about-text p + p { margin-top: 20px; }

    .about-stats {
      margin-top: 60px;
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 32px;
      border-top: 1px solid var(--border);
      padding-top: 40px;
    }
    .stat-num {
      font-family: var(--font-display);
      font-size: 40px;
      font-weight: 300;
      color: var(--gold2);
      line-height: 1;
    }
    .stat-label {
      font-family: var(--font-condensed);
      font-size: 11px;
      font-weight: 300;
      letter-spacing: 0.15em;
      text-transform: uppercase;
      color: var(--white-dim);
      margin-top: 8px;
    }

    /* =============================================
       PRODUCTS SECTION
    ============================================= */
    #produtos {
      padding: 140px 60px;
      background: var(--bg);
    }

    .products-header {
      text-align: center;
      margin-bottom: 80px;
    }
    .products-header .section-label {
      justify-content: center;
    }
    .products-header .section-label::before { display: none; }
    .products-header .section-h2 { text-align: center; }

    .products-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 24px;
      max-width: 1100px;
      margin: 0 auto;
    }

    .product-card {
      position: relative;
      background: var(--surface);
      border: 1px solid var(--border);
      overflow: hidden;
      transition: border-color 0.4s, transform 0.4s var(--transition);
      cursor: pointer;
    }
    .product-card:hover {
      border-color: var(--gold);
      transform: translateY(-6px);
    }

    .product-card-image {
      aspect-ratio: 16/9;
      overflow: hidden;
      position: relative;
    }
    /* ↓ IMAGENS DOS CARDS AQUI ↓ */
    .product-card-image img {
      width: 100%; height: 100%;
      object-fit: cover;
      filter: brightness(0.7) saturate(0.7);
      transition: transform 0.8s var(--transition), filter 0.5s;
    }
    .product-card:hover .product-card-image img {
      transform: scale(1.05);
      filter: brightness(0.8) saturate(1);
    }

    .product-card-image-overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(to top, var(--surface) 0%, transparent 60%);
    }

    .product-card-tag {
      position: absolute;
      top: 20px; left: 20px;
      font-family: var(--font-condensed);
      font-size: 9px;
      font-weight: 500;
      letter-spacing: 0.3em;
      text-transform: uppercase;
      background: var(--gold);
      color: var(--bg);
      padding: 6px 14px;
    }

    .product-card-body {
      padding: 36px 36px 40px;
    }

    .product-icon {
      width: 40px; height: 40px;
      margin-bottom: 24px;
      opacity: 0.7;
    }

    .product-title {
      font-family: var(--font-display);
      font-size: 30px;
      font-weight: 400;
      line-height: 1.2;
      color: var(--white);
      margin-bottom: 16px;
    }

    .product-desc {
      font-size: 14px;
      line-height: 1.8;
      color: var(--white-dim);
      margin-bottom: 32px;
    }

    .product-features {
      list-style: none;
      margin-bottom: 40px;
      display: flex;
      flex-direction: column;
      gap: 12px;
    }
    .product-features li {
      font-family: var(--font-condensed);
      font-size: 13px;
      font-weight: 300;
      letter-spacing: 0.05em;
      color: var(--white-dim);
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .product-features li::before {
      content: '';
      display: block;
      width: 20px; height: 1px;
      background: var(--gold);
      flex-shrink: 0;
    }

    .product-cta {
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .product-price {
      font-family: var(--font-display);
      font-size: 13px;
      font-weight: 300;
      color: var(--white-dim);
      font-style: italic;
    }

    /* =============================================
       PROOF / SHOWCASE SECTION
    ============================================= */
    #showcase {
      padding: 140px 60px;
      background: var(--bg3);
    }

    .showcase-header {
      margin-bottom: 80px;
    }

    .showcase-grid {
      display: grid;
      grid-template-columns: 1fr 1fr 1fr;
      gap: 2px;
    }

    .showcase-item {
      position: relative;
      overflow: hidden;
      aspect-ratio: 3/4;
      background: var(--surface);
    }
    /* ↓ IMAGENS SHOWCASE AQUI ↓ */
    .showcase-item img {
      width: 100%; height: 100%;
      object-fit: cover;
      transition: transform 0.8s var(--transition), filter 0.5s;
      filter: brightness(0.85) saturate(0.85);
    }
    .showcase-item:hover img {
      transform: scale(1.06);
      filter: brightness(1) saturate(1);
    }

    .showcase-item-info {
      position: absolute;
      bottom: 0; left: 0; right: 0;
      padding: 30px 24px;
      background: linear-gradient(to top, rgba(8,8,8,0.9) 0%, transparent 100%);
      transform: translateY(10px);
      opacity: 0;
      transition: all 0.4s var(--transition);
    }
    .showcase-item:hover .showcase-item-info {
      transform: translateY(0);
      opacity: 1;
    }

    .showcase-item-title {
      font-family: var(--font-condensed);
      font-size: 13px;
      font-weight: 400;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--white);
      margin-bottom: 4px;
    }
    .showcase-item-sub {
      font-family: var(--font-body);
      font-size: 12px;
      color: var(--gold);
    }

    .showcase-cta {
      text-align: center;
      margin-top: 80px;
    }
    .showcase-cta-text {
      font-family: var(--font-display);
      font-size: clamp(28px, 3vw, 46px);
      font-weight: 300;
      color: var(--white);
      margin-bottom: 12px;
    }
    .showcase-cta-sub {
      font-size: 15px;
      color: var(--white-dim);
      margin-bottom: 44px;
    }

    /* =============================================
       FINAL CTA SECTION
    ============================================= */
    #cta-final {
      position: relative;
      padding: 160px 60px;
      overflow: hidden;
      background: var(--bg);
      text-align: center;
    }

    .cta-bg {
      position: absolute;
      inset: 0;
      z-index: 0;
    }
    /* ↓ IMAGEM DO CTA FINAL AQUI ↓ */
    .cta-bg img {
      width: 100%; height: 100%;
      object-fit: cover;
      filter: brightness(0.2) saturate(0.4);
    }
    .cta-bg::after {
      content: '';
      position: absolute;
      inset: 0;
      background: radial-gradient(ellipse at center, rgba(196,154,90,0.08) 0%, rgba(8,8,8,0.5) 70%);
    }

    .cta-content {
      position: relative;
      z-index: 1;
    }

    .cta-eyebrow {
      font-family: var(--font-condensed);
      font-size: 11px;
      font-weight: 300;
      letter-spacing: 0.4em;
      text-transform: uppercase;
      color: var(--gold);
      margin-bottom: 28px;
    }

    .cta-h2 {
      font-family: var(--font-display);
      font-size: clamp(42px, 5.5vw, 82px);
      font-weight: 300;
      line-height: 1.05;
      color: var(--white);
      margin-bottom: 28px;
      max-width: 800px;
      margin-left: auto;
      margin-right: auto;
    }
    .cta-h2 em { font-style: italic; color: var(--gold2); }

    .cta-sub {
      font-size: 16px;
      color: var(--white-dim);
      line-height: 1.7;
      max-width: 520px;
      margin: 0 auto 60px;
    }

    .cta-btns {
      display: flex;
      gap: 20px;
      justify-content: center;
      flex-wrap: wrap;
    }

    /* =============================================
       FOOTER
    ============================================= */
    footer {
      background: var(--bg2);
      border-top: 1px solid var(--border);
      padding: 60px 60px 40px;
    }

    .footer-top {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 60px;
    }

    .footer-logo {
      display: flex;
      align-items: center;
      gap: 14px;
      margin-bottom: 20px;
    }
    .footer-logo img {
      width: 36px; height: 36px;
      object-fit: contain;
      filter: brightness(1.1);
    }
    .footer-logo-name {
      font-family: var(--font-condensed);
      font-size: 17px;
      font-weight: 500;
      letter-spacing: 0.2em;
      color: var(--white);
    }
    .footer-tagline {
      font-size: 13px;
      color: var(--white-dim);
      line-height: 1.6;
      max-width: 280px;
    }

    .footer-links {
      display: flex;
      gap: 80px;
    }
    .footer-col-title {
      font-family: var(--font-condensed);
      font-size: 10px;
      font-weight: 500;
      letter-spacing: 0.3em;
      text-transform: uppercase;
      color: var(--gold);
      margin-bottom: 20px;
    }
    .footer-col-links {
      list-style: none;
      display: flex;
      flex-direction: column;
      gap: 12px;
    }
    .footer-col-links a {
      font-size: 13px;
      color: var(--white-dim);
      transition: color 0.3s;
    }
    .footer-col-links a:hover { color: var(--white); }

    .footer-bottom {
      border-top: 1px solid var(--border);
      padding-top: 30px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .footer-copyright {
      font-family: var(--font-condensed);
      font-size: 11px;
      letter-spacing: 0.1em;
      color: rgba(250,250,248,0.25);
    }
    .footer-made {
      font-family: var(--font-condensed);
      font-size: 11px;
      letter-spacing: 0.05em;
      color: rgba(250,250,248,0.2);
    }
    .footer-made span { color: var(--gold); }

    /* Gold divider line */
    .gold-line {
      display: block;
      width: 100%;
      height: 1px;
      background: linear-gradient(to right, transparent, var(--gold), transparent);
      opacity: 0.4;
      margin: 0;
    }

    /* =============================================
       LIGHTBOX
    ============================================= */
    .lightbox {
      position: fixed;
      inset: 0;
      background: rgba(8,8,8,0.97);
      z-index: 2000;
      display: flex;
      align-items: center;
      justify-content: center;
      opacity: 0;
      pointer-events: none;
      transition: opacity 0.4s;
      padding: 40px;
    }
    .lightbox.open {
      opacity: 1;
      pointer-events: all;
    }
    .lightbox img {
      max-width: 90vw;
      max-height: 90vh;
      object-fit: contain;
      width: auto;
      height: auto;
    }
    .lightbox-close {
      position: absolute;
      top: 30px; right: 40px;
      font-family: var(--font-condensed);
      font-size: 12px;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--white-dim);
      cursor: pointer;
      transition: color 0.3s;
    }
    .lightbox-close:hover { color: var(--gold); }

    /* =============================================
       ANIMATIONS
    ============================================= */
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    /* =============================================
       RESPONSIVE
    ============================================= */
    @media (max-width: 1100px) {
      nav { padding: 24px 36px; }
      nav.scrolled { padding: 16px 36px; }
      .nav-links { gap: 32px; }
      #hero { }
      .hero-content { padding: 0 36px 80px; }
      #gallery { padding: 80px 36px; }
      .gallery-grid {
        grid-template-columns: repeat(6, 1fr);
      }
      .gallery-item:nth-child(1) { grid-column: 1/7; grid-row: 1/2; }
      .gallery-item:nth-child(2) { grid-column: 1/4; grid-row: 2/3; }
      .gallery-item:nth-child(3) { grid-column: 4/7; grid-row: 2/3; }
      .gallery-item:nth-child(4) { grid-column: 1/4; grid-row: 3/4; }
      .gallery-item:nth-child(5) { grid-column: 4/7; grid-row: 3/4; }
      .gallery-item:nth-child(6) { grid-column: 1/7; grid-row: 4/5; }
      .gallery-item:nth-child(7) { grid-column: 1/4; grid-row: 5/6; }
      .gallery-item:nth-child(8) { grid-column: 4/7; grid-row: 5/6; }
      .gallery-item:nth-child(9) { grid-column: 1/4; grid-row: 6/7; }
      .gallery-item:nth-child(10) { grid-column: 4/7; grid-row: 6/7; }
      #about { padding: 80px 36px; gap: 60px; }
      #produtos { padding: 80px 36px; }
      #showcase { padding: 80px 36px; }
      #cta-final { padding: 120px 36px; }
      footer { padding: 60px 36px 36px; }
    }

    @media (max-width: 800px) {
      nav { padding: 20px 24px; }
      nav.scrolled { padding: 14px 24px; }
      .nav-links { display: none; }
      .nav-cta { display: none; }

      .hero-content { padding: 0 24px 70px; }
      .hero-scroll { display: none; }

      #gallery { padding: 70px 16px; }
      .gallery-header { flex-direction: column; gap: 10px; }
      .gallery-grid {
        grid-template-columns: 1fr 1fr;
        gap: 8px;
      }
      .gallery-item:nth-child(1) { grid-column: 1/3; }
      .gallery-item:nth-child(2) { grid-column: 1/2; }
      .gallery-item:nth-child(3) { grid-column: 2/3; }
      .gallery-item:nth-child(4) { grid-column: 1/2; }
      .gallery-item:nth-child(5) { grid-column: 2/3; }
      .gallery-item:nth-child(6) { grid-column: 1/3; }
      .gallery-item:nth-child(7) { grid-column: 1/2; }
      .gallery-item:nth-child(8) { grid-column: 2/3; }
      .gallery-item:nth-child(9) { grid-column: 1/2; }
      .gallery-item:nth-child(10) { grid-column: 2/3; }
      [class*="grid-row"] { grid-row: auto; }

      #about {
        grid-template-columns: 1fr;
        gap: 50px;
        padding: 70px 24px;
      }
      .about-visual { max-width: 400px; aspect-ratio: 4/3; }
      .about-visual-badge { right: -10px; bottom: -10px; width: 110px; height: 110px; }
      .badge-number { font-size: 36px; }
      .about-stats { grid-template-columns: repeat(3, 1fr); }

      .products-grid { grid-template-columns: 1fr; gap: 20px; }
      #produtos { padding: 70px 24px; }

      .showcase-grid { grid-template-columns: 1fr 1fr; }
      .showcase-item:nth-child(3) { display: none; }
      #showcase { padding: 70px 24px; }

      #cta-final { padding: 100px 24px; }
      .cta-btns { flex-direction: column; align-items: center; }

      footer { padding: 50px 24px 30px; }
      .footer-top { flex-direction: column; gap: 40px; }
      .footer-links { gap: 40px; }
      .footer-bottom { flex-direction: column; gap: 12px; text-align: center; }
    }

    /* Mobile hamburger */
    .nav-menu-btn {
      display: none;
      flex-direction: column;
      gap: 5px;
      cursor: pointer;
      padding: 4px;
    }
    .nav-menu-btn span {
      display: block;
      width: 22px; height: 1px;
      background: var(--white);
      transition: all 0.3s;
    }
    @media (max-width: 800px) {
      .nav-menu-btn { display: flex; }
    }
  </style>
</head>
<body>

<!-- Grain overlay for premium texture -->
<div class="grain"></div>

<!-- Custom cursor (desktop only) -->
<div class="cursor" id="cursor"></div>
<div class="cursor-ring" id="cursorRing"></div>

<!-- Lightbox -->
<div class="lightbox" id="lightbox">
  <div class="lightbox-close" onclick="closeLightbox()">Fechar ✕</div>
  <img id="lightboxImg" src="" alt="" />
</div>

<!-- =============================================
     NAVIGATION
============================================= -->
<nav id="navbar">
  <div class="nav-logo">
    <!-- ↓ LOGO AQUI ↓ -->
    <img src="images/logo.jpg" alt="Trennus Studio Logo" />
    <div class="nav-logo-text">TRENNUS <span>STUDIO</span></div>
  </div>

  <ul class="nav-links">
    <li><a href="#gallery">Portfólio</a></li>
    <li><a href="#about">Estúdio</a></li>
    <li><a href="#produtos">Produtos</a></li>
    <li><a href="#showcase">Renders</a></li>
  </ul>

  <!-- ↓ LINK CTA NAVBAR — TROCAR SE NECESSÁRIO ↓ -->
  <a href="https://pay.kiwify.com.br/7OHvO2V" class="nav-cta" target="_blank">Acessar Curso</a>

  <div class="nav-menu-btn" onclick="toggleMenu()">
    <span></span><span></span><span></span>
  </div>
</nav>

<!-- =============================================
     HERO SECTION
============================================= -->
<section id="hero">
  <div class="hero-bg">
    <!-- ↓ IMAGEM HERO PRINCIPAL — TROCAR AQUI ↓ -->
    <img src="images/render7.jpg" alt="Trennus Studio — Renderização Arquitetônica com IA" />
  </div>
  <div class="hero-overlay"></div>
  <div class="hero-overlay-left"></div>

  <div class="hero-content">
    <div class="hero-eyebrow">Trennus Studio · Arquitetura & IA</div>

    <h1 class="hero-h1">
      Renderizações<br/>
      <em>Cinematográficas</em><br/>
      com Inteligência Artificial
    </h1>

    <p class="hero-sub">
      Crie imagens ultra realistas, renders profissionais e projetos arquitetônicos impressionantes em minutos — usando o poder da IA.
    </p>

    <div class="hero-btns">
      <!-- ↓ LINK CURSO ↓ -->
      <a href="https://pay.kiwify.com.br/7OHvO2V" class="btn-primary" target="_blank">
        <span>Acessar Curso</span>
      </a>
      <!-- ↓ LINK PROMPTS ↓ -->
      <a href="https://pay.kiwify.com.br/1aQ8Ks5" class="btn-outline" target="_blank">
        Baixar 100 Prompts
      </a>
    </div>
  </div>

  <div class="hero-scroll">
    <span>Scroll</span>
    <div class="scroll-line"></div>
  </div>
</section>

<!-- Marquee -->
<div class="marquee-section">
  <div class="marquee-track">
    <!-- Duplicado para loop infinito -->
    <div class="marquee-item">Renderização com IA</div>
    <div class="marquee-item">ArchViz Premium</div>
    <div class="marquee-item">Arquitetura Moderna</div>
    <div class="marquee-item">Interiores Luxuosos</div>
    <div class="marquee-item">Fachadas Cinematográficas</div>
    <div class="marquee-item">100 Prompts Prontos</div>
    <div class="marquee-item">Renderização com IA</div>
    <div class="marquee-item">ArchViz Premium</div>
    <div class="marquee-item">Arquitetura Moderna</div>
    <div class="marquee-item">Interiores Luxuosos</div>
    <div class="marquee-item">Fachadas Cinematográficas</div>
    <div class="marquee-item">100 Prompts Prontos</div>
  </div>
</div>

<!-- =============================================
     GALLERY SECTION
============================================= -->
<section id="gallery">
  <div class="gallery-header">
    <div class="gallery-header-text">
      <div class="section-label reveal">Portfólio</div>
      <h2 class="section-h2 reveal reveal-delay-1">
        Imagens que<br/><em>impressionam</em>
      </h2>
    </div>
    <div class="gallery-count reveal reveal-delay-2">10</div>
  </div>

  <div class="gallery-grid">
    <!-- ↓ GALERIA — SUBSTITUIR IMAGENS AQUI ↓ -->
    <div class="gallery-item reveal" onclick="openLightbox('images/render1.jpg')">
      <img src="images/render1.jpg" alt="Render Fachada 1" loading="lazy" />
      <div class="gallery-item-overlay">
        <span class="gallery-item-label">Fachada Moderna · IA</span>
      </div>
    </div>

    <div class="gallery-item reveal reveal-delay-1" onclick="openLightbox('images/render4.jpg')">
      <img src="images/render4.jpg" alt="Render Fachada 2" loading="lazy" />
      <div class="gallery-item-overlay">
        <span class="gallery-item-label">Casa Duplex · Render IA</span>
      </div>
    </div>

    <div class="gallery-item reveal" onclick="openLightbox('images/render5.jpg')">
      <img src="images/render5.jpg" alt="Render Casa 3" loading="lazy" />
      <div class="gallery-item-overlay">
        <span class="gallery-item-label">Residência Nº 141 · IA</span>
      </div>
    </div>

    <div class="gallery-item reveal reveal-delay-1" onclick="openLightbox('images/render6.jpg')">
      <img src="images/render6.jpg" alt="Render Casa 4" loading="lazy" />
      <div class="gallery-item-overlay">
        <span class="gallery-item-label">Fachada Térrea · IA</span>
      </div>
    </div>

    <div class="gallery-item reveal reveal-delay-2" onclick="openLightbox('images/render7.jpg')">
      <img src="images/render7.jpg" alt="Render Casa 5" loading="lazy" />
      <div class="gallery-item-overlay">
        <span class="gallery-item-label">Casa com Piscina · IA</span>
      </div>
    </div>

    <div class="gallery-item reveal reveal-delay-1" onclick="openLightbox('images/render8.jpg')">
      <img src="images/render8.jpg" alt="Render Casa 6" loading="lazy" />
      <div class="gallery-item-overlay">
        <span class="gallery-item-label">Entrada Iluminada · IA</span>
      </div>
    </div>

    <div class="gallery-item reveal reveal-delay-2" onclick="openLightbox('images/render2.jpg')">
      <img src="images/render2.jpg" alt="Banheiro Suite" loading="lazy" />
      <div class="gallery-item-overlay">
        <span class="gallery-item-label">Banheiro Suíte · Interior IA</span>
      </div>
    </div>

    <div class="gallery-item reveal" onclick="openLightbox('images/render3.jpg')">
      <img src="images/render3.jpg" alt="Banheiro 2" loading="lazy" />
      <div class="gallery-item-overlay">
        <span class="gallery-item-label">Banheiro Lavanda · Interior IA</span>
      </div>
    </div>

    <div class="gallery-item reveal reveal-delay-1" onclick="openLightbox('images/interior3.jpg')">
      <img src="images/interior3.jpg" alt="Sala de Estar" loading="lazy" />
      <div class="gallery-item-overlay">
        <span class="gallery-item-label">Sala de Estar · Interior IA</span>
      </div>
    </div>

    <div class="gallery-item reveal reveal-delay-2" onclick="openLightbox('images/interior6.jpg')">
      <img src="images/interior6.jpg" alt="Cozinha" loading="lazy" />
      <div class="gallery-item-overlay">
        <span class="gallery-item-label">Cozinha Gourmet · Interior IA</span>
      </div>
    </div>
  </div>
</section>

<span class="gold-line"></span>

<!-- =============================================
     ABOUT / STUDIO SECTION
============================================= -->
<section id="about">
  <div class="about-visual reveal">
    <div class="about-visual-main">
      <!-- ↓ IMAGEM SOBRE — TROCAR AQUI ↓ -->
      <img src="images/render9.jpg" alt="Trennus Studio" />
    </div>
    <div class="about-visual-badge">
      <div class="badge-number">5+</div>
      <div class="badge-text">Anos de<br/>Experiência</div>
    </div>
  </div>

  <div class="about-content">
    <div class="section-label reveal">Sobre o Estúdio</div>
    <h2 class="section-h2 reveal reveal-delay-1">
      Arquitetura & IA<br/><em>sem limites</em>
    </h2>

    <div class="about-text reveal reveal-delay-2">
      <p>
        A <strong>Trennus Studio</strong> nasceu da fusão entre arquitetura de alto padrão e o poder transformador da Inteligência Artificial. Criamos renders cinematográficos, fachadas impactantes e interiores ultra realistas que impressionam clientes e vendem projetos.
      </p>
      <p>
        Com domínio profundo das ferramentas de IA mais avançadas do mercado, desenvolvemos métodos exclusivos que reduzem o tempo de produção de semanas para minutos — sem abrir mão da qualidade premium.
      </p>
      <p>
        Agora compartilhamos todo esse conhecimento no nosso <strong>Curso de Render com IA</strong> e no pack de <strong>100 Prompts</strong> prontos para usar.
      </p>
    </div>

    <div class="about-stats reveal reveal-delay-3">
      <div>
        <div class="stat-num">500+</div>
        <div class="stat-label">Projetos Renderizados</div>
      </div>
      <div>
        <div class="stat-num">100</div>
        <div class="stat-label">Prompts Prontos</div>
      </div>
      <div>
        <div class="stat-num">5min</div>
        <div class="stat-label">Por Render com IA</div>
      </div>
    </div>
  </div>
</section>

<span class="gold-line"></span>

<!-- =============================================
     PRODUTOS SECTION
============================================= -->
<section id="produtos">
  <div class="products-header">
    <div class="section-label reveal">Produtos Digitais</div>
    <h2 class="section-h2 reveal reveal-delay-1">
      Escolha seu<br/><em>próximo nível</em>
    </h2>
  </div>

  <div class="products-grid">

    <!-- CARD 1 — CURSO -->
    <div class="product-card reveal">
      <div class="product-card-image">
        <!-- ↓ IMAGEM CARD CURSO — TROCAR AQUI ↓ -->
        <img src="images/render1.jpg" alt="Curso de Render com IA" />
        <div class="product-card-image-overlay"></div>
        <div class="product-card-tag">Curso Completo</div>
      </div>
      <div class="product-card-body">
        <svg class="product-icon" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
          <rect x="4" y="8" width="32" height="24" rx="2" stroke="#C49A5A" stroke-width="1.5"/>
          <path d="M16 15l8 5-8 5V15z" stroke="#C49A5A" stroke-width="1.5" stroke-linejoin="round"/>
        </svg>
        <h3 class="product-title">Curso de Render<br/>com Inteligência Artificial</h3>
        <p class="product-desc">
          Do zero ao render profissional usando as melhores ferramentas de IA do mercado. Método passo a passo desenvolvido exclusivamente pela Trennus Studio.
        </p>
        <ul class="product-features">
          <li>Técnicas exclusivas de prompt engineering para arquitetura</li>
          <li>Ferramentas de IA mais utilizadas no mercado</li>
          <li>Renders de fachada, interior e paisagismo</li>
          <li>Suporte e comunidade exclusiva</li>
          <li>Acesso vitalício + atualizações gratuitas</li>
        </ul>
        <div class="product-cta">
          <span class="product-price">Acesso Vitalício</span>
          <!-- ↓ LINK CURSO ↓ -->
          <a href="https://pay.kiwify.com.br/7OHvO2V" class="btn-primary" target="_blank">
            <span>QUERO ACESSAR</span>
          </a>
        </div>
      </div>
    </div>

    <!-- CARD 2 — PROMPTS -->
    <div class="product-card reveal reveal-delay-2">
      <div class="product-card-image">
        <!-- ↓ IMAGEM CARD PROMPTS — TROCAR AQUI ↓ -->
        <img src="images/render5.jpg" alt="100 Prompts para Renderizar" />
        <div class="product-card-image-overlay"></div>
        <div class="product-card-tag">Pack Exclusivo</div>
      </div>
      <div class="product-card-body">
        <svg class="product-icon" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M8 12h24M8 20h16M8 28h20" stroke="#C49A5A" stroke-width="1.5" stroke-linecap="round"/>
          <circle cx="32" cy="28" r="5" stroke="#C49A5A" stroke-width="1.5"/>
          <path d="M30.5 28l1 1 2-2" stroke="#C49A5A" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        <h3 class="product-title">100 Prompts para<br/>Renderizar em 5 Minutos</h3>
        <p class="product-desc">
          Coleção curada com 100 prompts prontos para gerar renders arquitetônicos impressionantes em minutos. Copie, cole e surpreenda.
        </p>
        <ul class="product-features">
          <li>100 prompts testados e aprovados</li>
          <li>Categorias: fachada, interior, paisagismo, noturno</li>
          <li>Compatível com Midjourney, DALL-E, Stable Diffusion</li>
          <li>Arquivo digital — download imediato</li>
          <li>Bônus: guia de personalização de prompts</li>
        </ul>
        <div class="product-cta">
          <span class="product-price">Download Imediato</span>
          <!-- ↓ LINK PROMPTS ↓ -->
          <a href="https://pay.kiwify.com.br/1aQ8Ks5" class="btn-primary" target="_blank">
            <span>QUERO OS PROMPTS</span>
          </a>
        </div>
      </div>
    </div>

  </div>
</section>

<span class="gold-line"></span>

<!-- =============================================
     SHOWCASE / PROOF SECTION
============================================= -->
<section id="showcase">
  <div class="showcase-header">
    <div class="section-label reveal">Galeria de Interiores</div>
    <h2 class="section-h2 reveal reveal-delay-1">
      Cada detalhe,<br/><em>perfeito</em>
    </h2>
  </div>

  <div class="showcase-grid">
    <!-- ↓ IMAGENS SHOWCASE — TROCAR AQUI ↓ -->
    <div class="showcase-item reveal" onclick="openLightbox('images/interior1.jpg')">
      <img src="images/interior1.jpg" alt="Interior Suíte" loading="lazy" />
      <div class="showcase-item-info">
        <div class="showcase-item-title">Suíte Principal</div>
        <div class="showcase-item-sub">Interior · Render IA</div>
      </div>
    </div>
    <div class="showcase-item reveal reveal-delay-1" onclick="openLightbox('images/interior2.jpg')">
      <img src="images/interior2.jpg" alt="Interior Banheiro" loading="lazy" />
      <div class="showcase-item-info">
        <div class="showcase-item-title">Sala de Estar</div>
        <div class="showcase-item-sub">Interior · Render IA</div>
      </div>
    </div>
    <div class="showcase-item reveal reveal-delay-2" onclick="openLightbox('images/interior4.jpg')">
      <img src="images/interior4.jpg" alt="Interior Sala" loading="lazy" />
      <div class="showcase-item-info">
        <div class="showcase-item-title">Living Premium</div>
        <div class="showcase-item-sub">Interior · Render IA</div>
      </div>
    </div>
  </div>

  <div class="showcase-cta reveal">
    <p class="showcase-cta-text">Pronto para criar assim?</p>
    <p class="showcase-cta-sub">Aprenda a dominar a IA para renderizações em nível profissional.</p>
    <div style="display:flex;gap:20px;justify-content:center;flex-wrap:wrap;">
      <!-- ↓ LINKS CTA ↓ -->
      <a href="https://pay.kiwify.com.br/7OHvO2V" class="btn-primary" target="_blank">
        <span>Acessar o Curso</span>
      </a>
      <a href="https://pay.kiwify.com.br/1aQ8Ks5" class="btn-outline" target="_blank">
        Ver os 100 Prompts
      </a>
    </div>
  </div>
</section>

<!-- =============================================
     FINAL CTA SECTION
============================================= -->
<section id="cta-final">
  <div class="cta-bg">
    <!-- ↓ IMAGEM DO CTA FINAL — TROCAR AQUI ↓ -->
    <img src="images/render10.jpg" alt="CTA Background" />
  </div>

  <div class="cta-content">
    <div class="cta-eyebrow reveal">Comece Agora · Trennus Studio</div>

    <h2 class="cta-h2 reveal reveal-delay-1">
      Transforme projetos<br/>em <em>obras de arte</em><br/>com IA
    </h2>

    <p class="cta-sub reveal reveal-delay-2">
      Junte-se a centenas de arquitetos, designers e entusiastas que já estão criando renders cinematográficos com Inteligência Artificial.
    </p>

    <div class="cta-btns reveal reveal-delay-3">
      <!-- ↓ LINKS FINAIS ↓ -->
      <a href="https://pay.kiwify.com.br/7OHvO2V" class="btn-primary" target="_blank">
        <span>Acessar o Curso Completo</span>
      </a>
      <a href="https://pay.kiwify.com.br/1aQ8Ks5" class="btn-outline" target="_blank">
        Baixar 100 Prompts
      </a>
    </div>
  </div>
</section>

<!-- =============================================
     FOOTER
============================================= -->
<footer>
  <div class="footer-top">
    <div>
      <div class="footer-logo">
        <!-- ↓ LOGO FOOTER ↓ -->
        <img src="images/logo.jpg" alt="Trennus Studio" />
        <div class="footer-logo-name">TRENNUS STUDIO</div>
      </div>
      <p class="footer-tagline">
        Renderizações arquitetônicas cinematográficas com Inteligência Artificial. Elevando o padrão visual da arquitetura.
      </p>
    </div>

    <div class="footer-links">
      <div>
        <div class="footer-col-title">Produtos</div>
        <ul class="footer-col-links">
          <!-- ↓ LINKS FOOTER ↓ -->
          <li><a href="https://pay.kiwify.com.br/7OHvO2V" target="_blank">Curso de Render com IA</a></li>
          <li><a href="https://pay.kiwify.com.br/1aQ8Ks5" target="_blank">100 Prompts</a></li>
        </ul>
      </div>
      <div>
        <div class="footer-col-title">Estúdio</div>
        <ul class="footer-col-links">
          <li><a href="#about">Sobre</a></li>
          <li><a href="#gallery">Portfólio</a></li>
          <li><a href="#showcase">Interiores</a></li>
        </ul>
      </div>
    </div>
  </div>

  <div class="footer-bottom">
    <div class="footer-copyright">
      © 2026 Trennus Studio · Todos os direitos reservados
    </div>
    <div class="footer-made">
      Arquitetura × <span>IA</span> × Criatividade
    </div>
  </div>
</footer>

<!-- =============================================
     JAVASCRIPT
============================================= -->
<script>
  // ── Custom Cursor ──
  const cursor = document.getElementById('cursor');
  const ring = document.getElementById('cursorRing');
  let mouseX = 0, mouseY = 0, ringX = 0, ringY = 0;

  document.addEventListener('mousemove', e => {
    mouseX = e.clientX; mouseY = e.clientY;
    cursor.style.left = mouseX + 'px';
    cursor.style.top = mouseY + 'px';
  });

  function animateRing() {
    ringX += (mouseX - ringX) * 0.12;
    ringY += (mouseY - ringY) * 0.12;
    ring.style.left = ringX + 'px';
    ring.style.top = ringY + 'px';
    requestAnimationFrame(animateRing);
  }
  animateRing();

  document.querySelectorAll('a, button, .gallery-item, .product-card, .showcase-item').forEach(el => {
    el.addEventListener('mouseenter', () => {
      cursor.style.width = '14px';
      cursor.style.height = '14px';
      ring.style.width = '56px';
      ring.style.height = '56px';
    });
    el.addEventListener('mouseleave', () => {
      cursor.style.width = '8px';
      cursor.style.height = '8px';
      ring.style.width = '36px';
      ring.style.height = '36px';
    });
  });

  // ── Navbar scroll ──
  const navbar = document.getElementById('navbar');
  window.addEventListener('scroll', () => {
    if (window.scrollY > 60) navbar.classList.add('scrolled');
    else navbar.classList.remove('scrolled');
  });

  // ── Reveal on scroll ──
  const reveals = document.querySelectorAll('.reveal');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
      }
    });
  }, { threshold: 0.1, rootMargin: '0px 0px -60px 0px' });

  reveals.forEach(el => observer.observe(el));

  // ── Lightbox ──
  function openLightbox(src) {
    document.getElementById('lightboxImg').src = src;
    document.getElementById('lightbox').classList.add('open');
    document.body.style.overflow = 'hidden';
  }
  function closeLightbox() {
    document.getElementById('lightbox').classList.remove('open');
    document.body.style.overflow = '';
  }
  document.getElementById('lightbox').addEventListener('click', function(e) {
    if (e.target === this) closeLightbox();
  });
  document.addEventListener('keydown', e => {
    if (e.key === 'Escape') closeLightbox();
  });

  // ── Mobile menu placeholder ──
  function toggleMenu() {
    // Mobile menu — adicionar lógica se necessário
  }

  // ── Parallax hero ──
  window.addEventListener('scroll', () => {
    const hero = document.querySelector('.hero-bg img');
    if (hero) {
      const scroll = window.scrollY;
      hero.style.transform = `scale(1) translateY(${scroll * 0.3}px)`;
    }
  });
</script>

</body>
</html>
